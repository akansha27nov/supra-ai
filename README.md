# Supra AI — AI-Assisted Supplier Compliance Screening

**Capstone project**

## Scenario

**Client:** Chleo, Head of Procurement and Supply Chain Operations at a mid-sized European omnichannel retailer (~200 employees, ~2,000 active SKUs, consumer electronics sourced from EU and non-EU suppliers).

**Problem:** Supplier compliance documentation (CE/RoHS certificates, test reports, declarations of conformity) is spread across PDFs, spreadsheets, and email, and checked manually. Chleo needs a faster way to identify incomplete, expired, or inconsistent supplier documentation and prioritise which products need human review — without an AI system making the compliance decision itself.

**Solution shape:** an AI-assisted screening layer that extracts structured data from supplier certificates, checks it against deterministic compliance rules and the internal SKU catalog, and surfaces prioritised, evidence-linked findings on a dashboard for human reviewers. The AI extracts; a transparent, auditable rule engine decides; a human approves.

## Repository structure

```
supra-ai/
├── research/                  # Sector research, opportunity/risk mapping, use cases
│   ├── sector_research.md
│   ├── opportunities_risks.md
│   └── use_cases.md
├── data/
│   ├── sample_pdfs/            # 7 real, in-scope test certificates (electronics only)
│   ├── ground_truth.json       # Labeled expected extraction + audit result per certificate
│   ├── skus.json                # Internal SKU catalog: mandatory standards, lead thresholds
│   ├── tableau_export.csv       # Real pipeline output, regenerated by trace_sample.py
├── n8n/
│   ├── Supra AI - PDF-Only Compliance Document Auditor.json   # n8n workflow
│   ├── workflow_documentation.md
│   └── n8n_workflow.png
├── langsmith/
│   ├── trace_sample.py          # Full pipeline: PDF -> LLM extraction -> rule engine -> traced + evaluated
│   └── langsmith_documentation.md
├── dashboard/
│   ├── Supra AI Compliance Auditor.twbx   # Tableau dashboard
│   ├── Supra AI Compliance Dashboard.png
│   └── dashboard_documentation.md
├── cost_estimation/
│   ├── cost_analysis.md
│   └── timeline_estimate.md
├── feedback/
│   └── round1_decision.md       
├── generate_test_certs.py       # Script used to generate the synthetic test certificate PDFs
├── requirements.txt
├── .env.example
```

## Setup

1. Clone/unzip the repository.
2. Create a Python environment and install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
3. Copy `.env.example` to `.env` and fill in real credentials (OpenAI API key, LangSmith API key, and Notion/Telegram credentials if running the full n8n workflow).

## Running the pipeline

```bash
python langsmith/trace_sample.py
```

This runs the real extraction + rule-based screening pipeline against all 7 real certificate PDFs in `data/sample_pdfs/`, prints an expected-vs-actual comparison against `data/ground_truth.json`, and regenerates `data/tableau_export.csv` from the real output. Every run is traced in LangSmith (project name set via `LANGCHAIN_PROJECT` in `.env`) — see `langsmith/langsmith_documentation.md` for what's traced and why.

**Current validated result: 7/7 (100%) agreement with labeled ground truth**, using real PDF text and a live LLM extraction call — see `langsmith_documentation.md` for the honest caveat on what this figure does and doesn't prove.

## Other deliverables

- **Dashboard**: open `dashboard/Supra AI Compliance Auditor.twbx` in Tableau (Desktop or Public), connected to `data/tableau_export.csv`. See `dashboard_documentation.md` for what each view shows.
- **n8n workflow**: import `n8n/Supra AI - PDF-Only Compliance Document Auditor.json` into n8n. See `workflow_documentation.md` for the architecture, what was fixed during testing, and current limitations.
- **Research pack**: `research/` covers sector context (with real, sourced statistics from McKinsey's 2026 State of AI report and Stanford HAI's AI Index), opportunity/risk mapping, and the three use cases this project is built around.
- **Cost/timeline**: `cost_estimation/` covers real measured AI inference cost (~$0.0004/certificate), a stated volume assumption, illustrative build cost, and a phased 4-week pilot timeline.
